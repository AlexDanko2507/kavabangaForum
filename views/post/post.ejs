<%- include('../layout/header.ejs')%>

<div class="main">
  <div class="container">
    <div class="content">

      <div class="post">
        <div class="top">
          <h2>
            <%=post.title %>
            <% if (post.owner.id === user.id) { %>
              <a style="color: black;text-decoration: none;" href="/post/edit/<%=post.id %>">✎</a>
              <a class="del-post" id="<%=post.id %>" style="color: black;text-decoration: none;">🞪</a>
              <% } %>
          </h2>
        </div>
        <div class="body">
          <%-post.body %>
        </div>
        <div class="bottom">
              <div class="author">
                Автор:
                <a href="/users/<%=post.owner.login %>">
                  <%=post.owner.login %>
                </a>
              </div>
              <% if (user.id) { %>
                <a class="likes" id="<%=post.id %>" data-url="<%=post.url %>" href="">Like:
                <%=post.likeCount %>
              <% } else {%>
                <p style="margin-bottom: auto;">Like:
                <%=post.likeCount %>
                </p>
              <% } %>
            </a>
        </div>
      </div>

      <div class="comments" id="<%=post.id %>">
        <h3>Комментарии</h3>
        <div class="comment-list">
          <%- include('../layout/comments.ejs', {items: comments}) %>


            <form class="comment">
              <textarea name="body"></textarea>
              <div class="buttons">
                <button class="cancel secondary">Отмена</button>
                <button class="send">Отправить</button>
              </div>
            </form>
        </div>
      </div>



    </div>
        <%- include('../layout/sidebar.ejs')%>

    </div>
</div>

<%- include('../layout/footer.ejs')%>